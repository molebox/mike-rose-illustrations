import React from 'react';

const Gallery = () => (
	<div
		style={{
			display: 'flex',
			justifyContent: 'center',
			alignItems: 'center',
		}}
	>
		<h1>This is the Gallery page</h1>
	</div>
);

export default Gallery;
